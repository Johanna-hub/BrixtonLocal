// Native platforms (not web – web should use `index.web.js` instead)
import React from 'react';
// This is a stub. TODO: Making `Link` work with prototyping features in Sketch/Figma, etc would be really nice

const MockLink = (({ children }) => (
  <>
    {children}
  </>
));

export default MockLink;
