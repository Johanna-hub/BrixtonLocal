const colors = {
  // colour scales here:
  greens: ['#ae1', '#1e1'],
  reds: ['#f11', '#f54'],
};

colors.red = colors.reds[5];

const fontSizes = [56, 48, 36, 24, 20, 16, 14];

const space = [0, 4, 8, 16, 32, 64, 128];

export default {
  space,
  fontSizes,
  colors,
};
